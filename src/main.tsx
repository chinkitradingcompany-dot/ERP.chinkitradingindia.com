import React from 'react'
import ReactDOM from 'react-dom/client'
import ERPApp from './ERPApp.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ERPApp />
  </React.StrictMode>,
)
